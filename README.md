# Task API

A simple Task API to implement CRUD operations such as:

- View all tasks
- View one task
- Add a task
- Update a task
- Delete a task

I connected the API to a DB(SQLite3) to remove in memory storage and add durability